# Changelog

## 2026-08-06 选题任务进度与失败状态

### 发现的问题

- 选题生成包含多轮模型事实校验，实际可能运行十几分钟；页面此前只能显示“排队中/处理中”，看不到当前阶段和进度。
- 失败批次在页面重新加载时会自动再次提交，容易造成重复等待；旧数据库补列后，历史失败记录还会显示成默认的排队状态。

### 修复内容

- 选题批次增加进度百分比、阶段、说明文字，并在复刻工作台展示“读取资料、生成选题、事实校验、保存结果”四步状态。
- 失败批次改为保留失败原因，由用户点击“重新生成选题”后才重试。
- 旧数据库迁移会把历史完成批次标为 100%，把历史失败批次标为失败；未修改任何提示词。

### 验证

- 前端 TypeScript 检查和生产构建通过。
- 后端镜像编译检查、Docker Compose 健康检查和登录/选题状态接口通过。

## 2026-08-06 事实校验误判优化

### 发现的问题

- 事实校验器要求整句原样出现在资料卡中，模型把“顾客真实反馈”自然改写成“真实顾客反馈日常”时会被误判为无依据事实。

### 修复内容

- 对人物互动和绝对表述改为检查短语证据是否在资料中有序出现，允许已有事实的合理改写；数字和同行/市场比较仍保持严格检查。
- 增加自然改写通过、无依据历史事件继续拦截的回归测试；未修改任何提示词。

### 验证

- 后端测试 28 项全部通过，服务健康检查通过。

## 2026-08-06 选题生成链路修复

### 发现的问题

- 资料卡保存后，选题接口把模型生成和事实校验同步放在一次 HTTP 请求中；云端 OpenShip 默认 60 秒上游超时，前端收到 504 时数据库还没有写入选题。
- 前端将失败的请求当作“没有选题”，没有展示任务状态，也无法自然重试同一份账号资料。

### 修复内容

- 选题生成改为 Redis/RQ 后台任务：接口快速创建批次并返回，Worker 负责模型生成、事实校验和写入 20 个选题。
- `topic_batches` 增加 `queued`、`running`、`completed`、`failed` 状态、错误信息和任务编号；已有数据库记录自动按已完成批次迁移。
- 复刻工作台轮询批次状态，显示排队、生成和失败原因；失败时可以重新生成，同一资料卡可以再次尝试。
- 增加 API、Worker 数据写入和旧数据库迁移测试；未修改任何提示词。

### 验证

- 后端测试 27 项通过。
- 前端 TypeScript 检查、生产构建和差异检查通过。

## 2026-08-03 验收与修复

### 本轮发现并修复

- 移动端拍摄脚本原本继续使用桌面五列表格，`390px` 视口下右侧字段被截断，且没有足够明确的横向阅读提示。
- 仅在窄屏样式中将脚本表格改为带字段标签的分段表格，保留横线、竖线、段次和内容阶段关系；桌面端五列表格保持不变。
- 新建账号资料时，原先只有笼统说明和整段示例，使用者无法快速判断哪些信息会影响后续选题与脚本。第一次调整将提示做成八格字段矩阵，视觉过重且示例标记过密；现改为四个自然写作要点（行业与主营、受众与目标、素材与条件、调性与边界）。下方完整示例恢复为原先的整段文字与浅绿色示例区，不添加额外标记。仅调整前端展示，不改变资料卡接口、追问逻辑或提示词。

### 此前修复并在本轮复测通过

- 登录页重做为带品牌头部、工作空间登录表单和响应式布局的页面；修复登录成功后不能进入工作台的问题。
- 加入管理员/普通用户权限、独立工作空间、设置页用户管理，并验证管理员设置接口与普通用户隔离。
- 首页与创作记录合并；单条视频拆解与复刻工作台分离；复刻进入前选择账号资料卡。
- 报告页完成信息层级、头部元数据、板块密度、断行和导出长图清晰度调整；导出倍率保持为 3x。
- 前端展示统一使用“复刻”等产品用语；全选控件、脚本表格边框、脚本版本历史、重新生成、复制和导出图片均完成调整。

### 本轮验证

- 桌面端和移动端逐页打开检查：首页、登录、拆解报告、资料卡选择、复刻工作台、脚本详情、管理设置五个页签及提示词页内七个能力页签。
- 复刻工作台验证 20 个选题、单选/取消选择、生成按钮禁用与启用、脚本展开/收起、版本控件、复制和导出入口。
- `390px` 移动端页面无横向溢出；修复后的脚本表格各字段完整可读。
- `/health/ready`、未登录认证、管理员/普通用户权限、报告工作空间隔离接口复核通过；未触发真实模型生成或配置保存。
- 前端 `npm run check`、`npm run build`、`git diff --check` 通过；后端测试此前已通过 25 项。

## v0.4.0-dev - 2026-08-01

### Added

- 服务端 Session Cookie 登录与 Argon2id 密码哈希。
- 管理员和普通用户角色，以及每个用户独立的工作空间。
- 设置页用户管理、新建用户、删除用户和管理员重设密码。
- 用户自行修改密码、退出登录和失效会话清理。

### Security

- 所有资料卡、拆解、选题和脚本接口按当前工作空间强制过滤。
- 模型、Cookie、提示词和用户管理接口仅允许管理员访问。
- 升级前的本地数据自动归入初始管理员工作空间。

## v0.3.0-dev - 2026-07-28

本地完整产品候选版，尚待有效模型配置与抖音 Cookie 下的真实链接验收。

### Added

- 账号主页采集 50 条作品与 30 条均衡视频深拆。
- 高表现、近期和常态样本组合，以及元数据覆盖说明。
- 账号综合报告和账号对标选题。
- 一句话账号资料、必要追问、资料卡确认与多份保存。
- 选题编辑、单选/多选/全选脚本任务、失败项单独重试。
- 三个独立 RQ Worker、任务恢复、取消、重试和删除。
- 报告长图导出、完整脚本查看与复制。
- Docker Compose 本地运行环境和 Redis/数据库就绪检查。

### Security

- API Key 和抖音 Cookie 保存在本机数据库，设置页直接显示完整内容。
- 诊断信息脱敏，视频和运行时数据不进入 Git。

## v0.2.0 - 2026-07-27

### Added

- 单条抖音链接解析、元数据读取、视频下载和模型拆解闭环。
- 拆解报告、20 个选题和编导可拍稿脚本结构。
- FastAPI、SQLite、真实历史记录与工作台 API。
- 管理设置、模型连接测试和抖音 Cookie 有效性测试。

## v0.1.0 - 2026-07-26

### Added

- 首页、工作台、结果页、账号资料弹窗和管理员设置的前端视觉原型。
