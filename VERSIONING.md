# 版本管理规范

## 仓库边界

这个仓库同时保存账号项目的核心资料、研究依据、视觉实验和视频对标工具源码。

- 核心文档、研究源文件、工具源码和精选样片进入 Git。
- `node_modules`、构建目录、测试截图、逐帧截图和临时渲染不进入 Git。
- API Key、Cookie、`.env` 和其他认证信息不得进入 Git。
- `99_归档` 只保存历史依据，不作为当前方案的有效来源。

## 分支规则

- `main`：只保留已经验证、可继续开发的稳定节点。
- `codex/<topic>`：Codex 开发分支，例如 `codex/single-video-pipeline`。
- 一个分支只处理一个明确主题，完成构建和检查后再合并到 `main`。
- 不在功能分支顺带重构无关文件。

## 提交格式

采用简化的 Conventional Commits：

- `feat:` 新功能
- `fix:` 缺陷修复
- `style:` 只影响视觉样式
- `refactor:` 不改变行为的代码整理
- `docs:` 文档修改
- `test:` 测试与验证
- `chore:` 仓库、依赖和工具维护

每个提交应当能够独立说明、独立回退。敏感配置、生成物和无关格式化不得混入功能提交。

## 版本节点

- `v0.1.0`：前端视觉原型基线。
- `v0.2.0`：单条视频真实拆解闭环。
- `v0.3.0`：账号主页采集与账号研究闭环。
- `v0.4.0`：账号资料问诊与复刻闭环。
- `v1.0.0`：可稳定部署和日常使用的首个版本。

版本号使用语义化版本。`0.x` 阶段允许接口调整，但每个版本节点必须保留可运行的主流程。

## 合并前检查

1. `git status` 中没有意外文件。
2. 前端执行 `npm run build`。
3. 不包含 API Key、Cookie、令牌或本地绝对配置。
4. 关键页面完成桌面与移动端检查。
5. `CHANGELOG.md` 记录用户可感知的变化。

